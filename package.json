{
  "name": "lob-a-bunch",
  "version": "0.1.0",
  "description": "Adding the ability to lob a bunch of requests at once.",
  "main": "index.js",
  "scripts": {
    "fix": "standard --fix",
    "test": "istanbul cover --report lcov --dir test/reports/ mocha --recursive ./test/ --grep ./test/**/*.spec.js -- --colors --reporter spec",
    "precommit": "standard && npm run test",
    "dev": "node dev/"
  },
  "peerDependencies": {
    "bluebird": "^3.x",
    "lob": "^3.x"
  },
  "author": "Avi Goldman <me@avigoldman.com> (http://avigoldman.com)",
  "license": "ISC",
  "devDependencies": {
    "bluebird": "^3.5.0",
    "husky": "^0.13.3",
    "istanbul": "^0.4.5",
    "lob": "^3.9.0",
    "mocha": "^3.3.0",
    "standard": "^10.0.2"
  },
  "dependencies": {
    "chai": "^3.5.0",
    "lodash.clonedeep": "^4.5.0",
    "lodash.merge": "^4.6.0"
  }
}
